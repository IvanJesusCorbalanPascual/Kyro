<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kyro"
        tools:targetApi="31">

        <!-- Tu actividad de Temario -->
        <activity
            android:name=".TemarioActivity"
            android:exported="true"> <!-- Exported true si es la launcher, false si no -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Tu actividad de Calendario -->
        <activity
            android:name=".CalendarioActivity"
            android:exported="false" />

        <!-- Tu actividad de Temario Seleccionado -->
        <activity
            android:name=".TemarioSeleccionadoActivity"
            android:exported="false" />

    </application>

</manifest>
